Launch agent for complex, multistep tasks.

Agent types: {agents}

Use for: slash commands (e.g., /check-file path/to/file.py)
Don't use for: reading specific files (Read/Glob), class definitions (Glob), searching 2-3 files (Read)

Notes: parallel when possible, results not visible to user (summarize), stateless unless session_id, detailed description with expected return info, specify code vs research, proactive if agent description says so
